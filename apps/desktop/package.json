{
  "name": "@openx/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/build-server.mjs && tauri dev",
    "build": "tauri build",
    "build:debug": "tauri build --debug",
    "build:server": "node scripts/build-server.mjs",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0",
    "@yao-pkg/pkg": "^5.16.0",
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0",
    "@tauri-apps/plugin-shell": "^2.2.0"
  }
}
